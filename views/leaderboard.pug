doctype html
html(lang='de')
    include templates/head
    body
        include templates/cookieBanner
        // ======= Header =======
        include templates/header
        main#main
            // ======= Breadcrumbs =======
            section.no-section-after.breadcrumbs
                .container
                    .d-flex.justify-content-between.align-items-center
                        h2 Leaderboard
                        ol
                            li
                                a(href='/') Home
                            li Leaderboard
            section.leaderboard.inner-page.no-section-after
                .container
                    .section-title
                        img.guild-icon(src=levelData[0].guild.icon, alt='Leaderboard')
                        h2.mt-2 #{levelData[0].guild.name}
                    .row
                        .leaderboard-container.col-md-12
                            each levelUser in levelData
                                .leaderboard-item
                                    div.rank-container
                                        div.rank-text(class="rank-" + levelUser.position) Rang
                                        div.rank-number #{levelUser.position}
                                    div.avatar-container
                                        img.avatar(src=levelUser.user.avatar, alt=levelUser.user.username)
                                    div.username-container
                                        div #{levelUser.user.displayName}
                                    div.level-container
                                        div.level-text Level
                                        div.level-number #{levelUser.level}
                                    div.xp-container
                                        div.xp-text XP
                                        div.xp-number #{levelUser.cleanXp}






        // ======= Footer =======
        include templates/footer